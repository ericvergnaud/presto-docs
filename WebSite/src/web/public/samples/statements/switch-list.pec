define main as method doing:
    a = 3
    switch on a:
        when in [1, 3]:
            print "a = " + a
        when 17:
            print "a = 17"
        otherwise:
            print "a = other"