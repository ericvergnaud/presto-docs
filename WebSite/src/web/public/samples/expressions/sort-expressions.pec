define Entity as category with attribute name

define main as method doing:
    e1 = Entity with "Peter" as name
    e2 = Entity with "John" as name
    e3 = Entity with "Alan" as name
    list = [ e1, e2, e3 ]
    printLine "sorted -> " + sorted list with name[2:3] as key