define main as method doing:
    a = "some text"
    b = "some other text"
    c = "some text"
    printLine "a < b -> " + (a < b)
    printLine "a > b -> " + (a > b)
    printLine "a >= c -> " + (a >= c)
    printLine "a <= c -> " + (a <= c)