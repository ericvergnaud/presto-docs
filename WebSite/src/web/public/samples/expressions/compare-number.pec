define main as method doing:
    a = 2
    b = 3
    c = 2.0
    printLine "a < b -> " + (a < b)
    printLine "a > b -> " + (a > b)
    printLine "a >= c -> " + (a >= c)
    printLine "a <= c -> " + (a <= c)