define Person as category with attribute name

define main as method doing:
    p = Person with "John" as name
    print "p is an Integer -> " + (p is an Integer)
    print "p is a Person -> " + (p is a Person)